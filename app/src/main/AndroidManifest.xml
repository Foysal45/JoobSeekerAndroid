<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bdjobs.app">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BackgroundJob.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />

        <!-- facebook login related data start -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <!-- facebook login related data end -->

        <activity
            android:name=".SplashActivity"
            android:launchMode="singleTop"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GuestUserLanding.GuestUserJobSearchActivity" />
        <activity android:name=".Login.LoginBaseActivity" />
        <activity android:name=".SuggestiveSearch.SuggestiveSearchActivity" />

        <service
            android:name=".Notification.BdjobsFirebaseMessagingService"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".BroadCastReceivers.ConnectivityReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".LoggedInUserLanding.MainLandingActivity"
            android:label="@string/title_activity_main_landing" />
        <activity android:name=".Jobs.JobBaseActivity" />
        <activity android:name=".Registration.RegistrationBaseActivity" />
        <activity android:name=".FavouriteSearch.FavouriteSearchBaseActivity" />

        <receiver
            android:name=".BroadCastReceivers.BackgroundJobBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.bdjobs.dataBaseUpdateJob.jobComplete" />
            </intent-filter>
        </receiver>

        <activity android:name=".editResume.employmentHistory.EmploymentHistoryActivity" />
        <activity
            android:name=".editResume.BaseActivity"
            android:label="@string/title_activity_base" />
        <activity android:name=".editResume.personalInfo.PersonalInfoActivity"></activity>
    </application>

</manifest>